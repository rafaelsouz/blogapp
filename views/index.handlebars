<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao Blog do Node</h1>
  <p class="lead">Este é um Blog simples, feito com NodeJS + MongoDB + Express.</p>
  <hr class="my-4">
  <p>Aqui você vai ler sobre as ultimas novidades sobre o mundo da programação.</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>

<hr>
<h2>Ultimas postagens</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}} </small>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem feita</h5>
{{/each}}